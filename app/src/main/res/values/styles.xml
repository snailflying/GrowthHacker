<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="CustomTab" parent="Widget.Design.TabLayout" >
        <item name="tabIndicatorColor">@color/tab_indicator</item>
        <item name="tabTextColor">@color/tab_tv_normal</item>
        <item name="tabSelectedTextColor">@color/tab_tv_selected</item>
        <item name="tabBackground">@color/bg_tab_border_selecter</item>
        <item name="tabIndicatorHeight">0dp</item>
    </style>
    <!--<style name="CustomTab" parent="Widget.Design.TabLayout" >-->
        <!--<item name="tabIndicatorHeight">0dp</item>-->
    <!--</style>-->
    <style name="listView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:footerDividersEnabled">false</item>
        <item name="android:paddingRight">10dip</item>
        <item name="android:paddingLeft">10dip</item>
        <item name="android:paddingTop">10dip</item>
        <item name="android:clickable">false</item>
        <item name="android:listSelector">@color/translucent</item>
        <item name="android:background">@color/listViewBg</item>
        <item name="android:dividerHeight">1px</item>
        <item name="android:divider">@color/listViewDiv</item>
    </style>

</resources>
